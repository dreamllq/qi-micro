{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo/*"
  ],
  "scripts": {
    "publish-expose-types": "lerna run publish-expose-types --scope app --scope foundation"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3",
    "webpack-cli": "^5.1.4"
  }
}